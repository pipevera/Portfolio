---
interface Props {
   date: string
   title: string
   description: string
}
const {date, title, description } = Astro.props
---

<div class="relative bg-white/5 backdrop-blur-sm rounded-xl p-6 border border-white/10 hover:border-cyan-400/50 transition-all duration-500 hover:shadow-xl hover:shadow-cyan-400/10 group">
   <div class="space-y-3">
      <time class="inline-block px-3 py-1 text-sm font-medium text-cyan-400 bg-cyan-400/10 rounded-full border border-cyan-400/20">
         {date}
      </time>
      <h3 class="text-2xl md:text-3xl font-bold text-white group-hover:text-cyan-400 transition-colors duration-300">
         {title}
      </h3>
      <p class="text-base md:text-lg text-white/80 leading-relaxed">
         {description}
      </p>
   </div>
   
   <!-- Efecto de brillo en hover -->
   <div class="absolute inset-0 rounded-xl bg-gradient-to-r from-transparent via-cyan-400/5 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500 pointer-events-none"></div>
</div>
   
   